/*jQuery(document).ready(function(){
	var href = window.location.href;
	jQuery('.aios-roadmaps a').each(function(i,v){
		var array = jQuery(v).attr('href').split('/');
		array.pop();
		var this_a = array.pop();
		if(href.indexOf(this_a) > 0 ) {
			jQuery(v).addClass('active-link');
		}
	});
});
*/